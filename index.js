function takeANumber(line , name) {
  var subArray = [];
  var array =[];

  subArray.push(name);
  array.push(`Welcome, ${subArray[subArray.length - 1]}. You are number ${array.length} in line.`)

  }
  return subArray;
  return array;
}
